specVersion: 1.0.0
name: astar-starter
version: 0.0.1
runner:
  node:
    name: '@subql/node'
    version: '*'
  query:
    name: '@subql/query'
    version: '*'
description: Index stakers on dApp AstarDegen
repository: 'https://github.com/subquery/astar-subql-starters'
schema:
  file: ./schema.graphql
network:
  chainId: '0x9eb76c5184c4ab8679d2d5d819fdf90b9c001403e9e17da2e14b6d8aec4029c6'
  endpoint: 'wss://astar.api.onfinality.io/public-wss'
  dictionary: 'https://api.subquery.network/sq/subquery/astar-dictionary'
  chaintypes:
    file: ./dist/chaintypes.js
dataSources:
  - kind: substrate/Runtime
    startBlock: 2168680
    mapping:
      file: ./dist/index.js
      handlers:
# handlers on the events of the module assets works fine
#        - handler: handleEventAssets
#          kind: substrate/EventHandler
#          filter:
#            module: assets
# handlers on the events of the module balances works fine
#        - handler: handleEventBalances
#          kind: substrate/EventHandler
#          filter:
#            module: balances
# handlers on the events of the module utility works fine
#        - handler: handleEventUtility
#          kind: substrate/EventHandler
#          filter:
#            module: utility
# handlers on the events of the module dappsstaking doesn't work
        - handler: handleEventDappsStaking
          kind: substrate/EventHandler
          filter:
            module: dappsStaking
#        - handler: handleBondAndStake
#          kind: substrate/EventHandler
#          filter:
#            module: dappsstaking
#            method: BondAndStake
